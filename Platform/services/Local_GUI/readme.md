## Purpose

* Serves as the client-side UI for demos and running local tools, augmenting the web dashboard, which shows CVE database information and other documentation.

## Getting started

* To get started install the `requirements.txt` if any, using the command `pip install -r requirements.txt` 

* Then from terminal run `python main.py`
